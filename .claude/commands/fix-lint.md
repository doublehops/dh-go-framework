Run `make lint` and fix all reported issues in the codebase.

1. Run `make lint` and capture the full output
2. Analyse every issue reported
3. Fix each issue directly in the relevant source file
4. Re-run `make lint` to confirm all issues are resolved
5. Report a summary of what was changed
