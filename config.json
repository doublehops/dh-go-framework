{
  "host": {
    "port": "ENV:PORT"
  },
  "logging": {
    "writer": "stdout",
    "logLevel": "DEBUG",
    "outputFormat": "text"
  },
  "database": {
    "host": "ENV:MYSQL_HOST",
    "name": "ENV:MYSQL_DATABASE",
    "user": "ENV:MYSQL_USER",
    "password": "ENV:MYSQL_PASSWORD"
  }
}
